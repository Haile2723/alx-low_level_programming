#include "main.h"

/**
 *main - Check the code for ALX school student
 *Return: Always 0.
 */
int main(void)
{
int i;
if (i > 0)
{
printf("%d is positive\n", i);
}
else if (i < 0)
{
printf("%d is negative\n", i);
}
else
{
printf("%d is zero\n");
retun (0);
}
