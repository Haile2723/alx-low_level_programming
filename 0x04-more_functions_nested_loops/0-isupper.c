#include "main.h"
/**
 *_isupper - Print 1 if it is upper case , 0 otherwise
 *@c: the letter to be checked.
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
